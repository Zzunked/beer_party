

void handle_events_main_menu(float* accumulator, const float* timestep, SDL_Event* event, bool* running, 
        Scene current_scene);


void render_scene_main_menu(RenderWindow* window, Background* background);

